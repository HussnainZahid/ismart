import ContactContent from './ContactContent';

export const metadata = {
  title: 'Contact ISmart - Get Help & Support',
  description: 'Contact ISmart Customer Care for help with orders, returns, and general inquiries.',
};

export default function ContactPage() {
  return <ContactContent />;
}